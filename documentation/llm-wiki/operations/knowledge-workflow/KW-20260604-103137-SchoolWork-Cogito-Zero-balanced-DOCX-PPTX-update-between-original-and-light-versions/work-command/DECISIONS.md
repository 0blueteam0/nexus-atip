# Decisions

- Keep original files untouched and create v1.1 balanced copies.
- Use Python Office libraries via uv instead of global install because they were immediately available and sufficient.
- Use subagents for content/design judgment because user explicitly asked to find and use better agents/tools.
- Use 91 DOCX paragraphs and 14 PPTX slides as the middle point.
- Record visual-render limitation because soffice/libreoffice was not available in PATH.
