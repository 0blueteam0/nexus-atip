# Decision Log

- Do not promote the focused run to final AF generation because OpenCV accepted_document_like_count is 0 and privacy_review_status remains quarantine.
- Keep same-coordinate pseudonymized rewrite as the only allowed downstream transformation.
- Store no raw OCR values by default.
