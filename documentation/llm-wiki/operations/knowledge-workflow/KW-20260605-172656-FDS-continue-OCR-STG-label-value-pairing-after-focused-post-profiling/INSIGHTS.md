# Insights

- The focused run has OCR signal but still lacks automatically accepted document-like originals, so it should stay quarantine/manual-review only.
- Current real OCR field hints mostly produce direct date/amount token bboxes, not label-value pairs. More medical-form-like web originals are needed before high-confidence AF generation.
- User's preference should be interpreted as no visible safety labels or blocks in pixels; lineage/safety should remain in metadata and reports.
