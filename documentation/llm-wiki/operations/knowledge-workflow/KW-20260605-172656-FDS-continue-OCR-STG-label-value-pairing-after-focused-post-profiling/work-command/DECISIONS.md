# Decisions

- The focused run remains quarantine-only because OpenCV accepted count is zero.
- No visible mask/block/synthetic-only/not-for-submission artifacts are allowed in generated images.
- Raw OCR values remain non-retained; only redacted/hinted metadata is used.
- Label tokens are anchors; right-side values are the intended local substitution targets.
