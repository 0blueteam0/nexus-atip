# Tool Decision

- Used web search because the user explicitly requested searching for red team tools.
- Restricted evidence to official/primary sources for install and project identity.
- Added candidates as install/onboarding candidates instead of executable ToolProfiles.
- Kept `commands_executed_by_api=false` and `trusted_as_instruction=false` for every candidate.
- Preserved the existing required-six `tool_count` semantics.
