# Work Command Tooling

## Used

- `rg` for locating service import contracts.
- `Get-Content -Encoding UTF8` for Korean Markdown and source reads.
- `apply_patch` for all manual edits.
- Python venv for compile, regression, sanity, audit JSON, and completion blocker checks.
- `node --check` for frontend syntax.

## Not Used

- No active scanner, Docker, WSL, browser automation, or external endpoint call was used.
