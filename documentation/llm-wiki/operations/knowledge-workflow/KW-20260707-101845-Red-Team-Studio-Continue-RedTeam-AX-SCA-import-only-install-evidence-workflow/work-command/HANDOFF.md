# Handoff

SCA import-only install evidence is now implemented.

Read next:

- `record_sca_import_only_install_evidence` in `runtime/redteam_v2_models.py`.
- `/tool-install-version-evidence/sca-import-only` in `runtime/redteam_v2_api_router.py`.
- `test_v2_sca_import_only_install_evidence_records_operator_reviewed_sbom_without_execution`.
- `recordRedTeam2ScaImportOnlyInstallEvidence` in `reports.js`.

Next useful work: connect SCA submitted artifact to toolchain collection normalization and LLM agent summary, then push toward full six-tool E2E.
