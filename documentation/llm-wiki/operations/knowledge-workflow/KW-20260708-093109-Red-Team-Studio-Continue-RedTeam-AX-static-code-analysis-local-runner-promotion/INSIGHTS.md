# Insights

Bandit is beginner-friendly because it does not run Python code; it reads code and reports common risky patterns. It also fits the script factory workflow: generated helper scripts must be scanned before approval. The existing composite execution limit of 6 tools remains in place; tests now verify full preset catalog separately from a 6-tool execution subset.
