# TOOLING

Used:
- `rg` and UTF-8 file reads for inspection.
- `apply_patch` for scoped edits.
- pytest for API regression.
- `node --check` for frontend syntax.
- Project sanity scripts and accepted gate manifest.

This API is an approval workflow tool, not a scanner runner.
