# Tooling

Used:

- `rg` and UTF-8 `Get-Content` for inspection.
- `apply_patch` for file edits.
- `.venv\Scripts\python.exe` for sanity scripts, pytest, contracts, and accepted gates.
- `node --check` for frontend syntax.

Not used:

- Docker, WSL, OpenVAS, ZAP, network scanner, MCP execution.
- Evidence Card API mutation from the import planner.
