# Tooling

- `rg` for search.
- `apply_patch` for edits.
- Python py_compile and pytest for backend validation.
- `node --check` for frontend syntax.
- RedTeam AX sanity scripts for UI/audit/plan contracts.
- accepted gate manifest for 26-gate verification.
