# Ontology Edges

- Required analysis tool -> requires -> tool-specific LLM analysis agent.
- Toolchain result collection -> computes -> tool coverage, agent coverage, evidence candidate coverage.
- Agent coverage complete -> requires -> normalized result and agent id for each required tool.
- Frontend RedTeam2 -> displays -> Korean six-tool LLM agent coverage table.
- Report claim -> still requires -> approved Evidence and later Finding/Matrix gates.
