# Handoff

Next agent should continue with a longer focused run after manual source-scope tuning. Start from `scripts/collect_real_insurance_claim_sources.py` and `tests/test_real_web_source_collector.py`. For candidate generation, use `outputs/real_web_claim_sources_focused_no_noise_smoke3` as a tiny verified smoke artifact, not as final dataset scale.
