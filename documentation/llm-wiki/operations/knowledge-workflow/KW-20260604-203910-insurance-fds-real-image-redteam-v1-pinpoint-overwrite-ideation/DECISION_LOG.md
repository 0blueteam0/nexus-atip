# Decision Log

## D-001 Treat v3 as wrong basis

Decision: Do not use v3 family as the canonical continuation basis.
Reason: User clarified prior issue was coordinate/content reconstruction; real-image-redteam-v1 is the correct version.

## D-002 Preserve v1 lineage, replace editing mechanism

Decision: Keep real-image-redteam-v1 source lineage/quarantine structure, but replace ratio-based overlay with paired-NO pinpoint overwrite.

## D-003 Recommended stack

Decision: Use OCR + table/cell detection + SAM/SAM2 + deterministic OpenCV/Pillow overwrite first; VLM and diffusion only as assistive QA/background repair.
