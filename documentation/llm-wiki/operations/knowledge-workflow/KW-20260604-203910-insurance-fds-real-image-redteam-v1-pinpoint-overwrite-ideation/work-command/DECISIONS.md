# Decisions

## Keep real-image-redteam-v1 as basis

The user explicitly corrected the basis. v3 is not used for continuation because it involved coordinate/content reconstruction.

## Diagnose current v1 as structurally right but edit-mechanically wrong

The current v1 has real public candidate lineage, NO/AF split, masks, and quarantine metadata. However it uses ratio-based tamper boxes and obvious overlays.

## Model roles

Models locate/validate fields. Deterministic code performs the edit. Diffusion is limited to background inpaint, not semantic text generation.
