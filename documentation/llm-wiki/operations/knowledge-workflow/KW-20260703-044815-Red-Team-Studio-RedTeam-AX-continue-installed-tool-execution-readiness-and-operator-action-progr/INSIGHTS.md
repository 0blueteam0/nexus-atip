# Insights

A single blocker list is not enough for non-expert operators. The runtime boundary needs a structured next-action plan that says which step blocks tool execution, what the operator should do next, and which API/command verifies it. This improves frontend-guided execution without granting the AI new authority to run high-risk tools.
