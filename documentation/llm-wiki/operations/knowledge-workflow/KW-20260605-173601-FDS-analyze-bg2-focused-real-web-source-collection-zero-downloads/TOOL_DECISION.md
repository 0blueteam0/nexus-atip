# Tool Decision

- Used direct JSONL inspection for deterministic counts.
- Used pytest for collector regression tests.
- Used a single live fetch probe to verify URL quoting against a previously failing Korean-path image URL.
