# Tooling

- `python tools/knowledge_workflow.py start/close` for evidence gate.
- Python JSONL parsing for deterministic manifest statistics.
- `PYTHONPATH=src python -m pytest tests/test_real_web_source_collector.py -q` for collector regression verification.
- A live `fetch_bytes()` probe against the Korean-path KB Think image URL to confirm URL percent-encoding fixes the UnicodeEncodeError class.
