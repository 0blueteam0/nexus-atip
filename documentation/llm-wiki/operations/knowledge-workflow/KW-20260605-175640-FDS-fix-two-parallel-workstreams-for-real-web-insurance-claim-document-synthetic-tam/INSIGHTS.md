# Insights

- The user is not asking for another broad plan; they want the next work to be constrained to two parallel tracks.
- The data-generation track and the test-delay track should not be merged. Data quality/provenance work can proceed while test harness RCA fixes collection errors and slow fixtures.
- The visible mask/block/submission-invalid shortcut problem is both a user preference and a model-quality issue: visible artifacts can become shortcuts and reduce real FDS usefulness.
- The first test-harness blocker is not slowness but missing import target. Slowness appears after using the older four-test bundle, mainly in camera-image generation tests.
