# Tasks

1. Inspect existing AI SOC implementation seed and LangGraph composition.
2. Design a large foreground lane based on explicit agent modules rather than small isolated edits.
3. Implement deterministic module catalog with module ids, inputs, outputs, safety gates, candidate backends, and LangGraph node ownership.
4. Integrate module catalog into existing LangGraph graph spec and run assurance.
5. Generate reviewable JSON and Mermaid artifacts.
6. Add unit tests for module catalog and graph-module assurance.
7. Update README and evaluation protocol.
8. Run targeted tests, full unittest discovery, syntax check, and artifact structure inspection.
