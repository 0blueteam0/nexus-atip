# Ontology Edges

- RedTeam2 default analyst view -> uses -> Korean display labels
- Korean display labels -> hide by default -> raw API paths
- Korean display labels -> hide by default -> internal tool IDs
- Korean display labels -> hide by default -> execution policy tokens
- RedTeam2 browser verification -> produces -> DOM evidence artifact
- DOM evidence artifact -> supports -> RTA-COMP-076
- RTA-COMP-076 -> contributes to -> RedTeam AX completion audit matrix
- RedTeam AX completion audit matrix -> does not complete -> full RedTeam AX goal
