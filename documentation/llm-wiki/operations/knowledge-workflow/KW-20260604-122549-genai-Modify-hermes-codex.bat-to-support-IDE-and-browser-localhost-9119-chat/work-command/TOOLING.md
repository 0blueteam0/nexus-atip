# TOOLING

Tools used:
- `skill_view`: loaded `hermes-agent` procedural reference.
- `read_file`: inspected launcher and Hermes source notes.
- `search_files`: found dashboard `/chat` and `--tui` references.
- `patch`: modified `hermes-codex.bat` safely.
- `terminal`: ran Hermes help, batch files through Windows `cmd.exe //C`, HTTP route checks, and git status.
- `write_file`: recorded workflow evidence.

Important command nuance:
- In this Git Bash/MSYS terminal, use `cmd.exe //C "..."` rather than `cmd.exe /c "..."` for Windows batch verification.
