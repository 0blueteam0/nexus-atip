# Tool Decision

- Used unittest because implementation_seed already uses Python unittest.
- Used Mermaid `.mmd` because it is a lightweight, reviewable graph artifact that can be embedded in docs, browser dashboards, or future Kanban flow viewers without adding a rendering dependency.
- Preserved edge conditions as Mermaid comments so simple graph edges remain broadly compatible.
