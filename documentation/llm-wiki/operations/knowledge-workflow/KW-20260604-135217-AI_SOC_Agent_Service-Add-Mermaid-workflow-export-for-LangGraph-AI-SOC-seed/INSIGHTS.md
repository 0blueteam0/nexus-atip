# Insights

- The LangGraph seed now has three complementary review surfaces: JSON spec, actual run JSON, and Mermaid graph.
- Mermaid export is also useful for the parallel Hermes Kanban/LangGraph UX lane because Kanban can consume the same node/edge/state shape.
- Keeping conditions as comments avoids Mermaid renderer incompatibilities while preserving contract evidence.
