# Reviews

## Self Review

- Safety: Import path does not execute scanner commands and keeps `commands_executed_by_api=false`.
- Integrity: `source_path` and `sha256` are checked by existing importer before artifact storage.
- Traceability: Manifest import creates persisted toolchain metadata and runs compatible with collection Evidence flow.
- UI: Korean beginner copy distinguishes manifest import from local runner execution and inline operator paste.
- Tests: Router regression covers success and hash mismatch; sanity scripts cover UI strings and plan/audit contracts.

## Residual Risk

- Representative fixtures prove contract behavior, not real organizational scanner output approval.
- Real evidence must still be reviewed and approved by humans before Finding/report claims.
