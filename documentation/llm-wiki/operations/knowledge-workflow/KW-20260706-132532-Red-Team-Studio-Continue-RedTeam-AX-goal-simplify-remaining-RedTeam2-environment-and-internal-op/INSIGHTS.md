# Insights

- Collapsing admin panels was not enough; the collapsed summary and analyst next-step rows still leaked operational vocabulary.
- The most effective reduction came from changing display labels and fallback text, not backend/state naming.
- Browser DOM evidence is necessary here because static source still contains backend/admin/API strings that should remain for traceability.
- Future cleanup should target global navigation and common Report Studio legacy copy separately from RedTeam2 panel content.
