# Agent Roster

Primary executor: Codex.

System handoff target: Claude, via `handoff.ps1 provider` and `handoff.ps1 "redteam-ax" ...` after knowledge workflow close.

No subagents were used. No external scanner, Docker, or WSL operator action was delegated in this slice.
