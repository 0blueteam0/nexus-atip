# Insights

- The safe next OTRF step is not raw parser work; it is a reviewable adapter contract preserving access/license/PII gates.
- LangGraph can now be used as a real dependency in this Hermes venv.
- The first AI SOC LangGraph seed should operate on Evidence Package fixtures and produce a human-review brief only.
- Kanban/LangGraph UX belongs in a separate background lane because it touches Hermes/Kanban UI rather than AI_SOC implementation_seed.
