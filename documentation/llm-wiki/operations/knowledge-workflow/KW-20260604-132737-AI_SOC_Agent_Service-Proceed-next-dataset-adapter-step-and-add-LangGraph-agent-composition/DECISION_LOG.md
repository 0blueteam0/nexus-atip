# Decision Log

1. Install LangGraph after explicit user clarification.
2. Keep OTRF work contract-only and no-download.
3. Use actual LangGraph StateGraph compile/invoke instead of JSON-only mock.
4. Spawn Hermes Kanban/LangGraph UX exploration as a background one-shot cron job.
