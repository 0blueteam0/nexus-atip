# Decisions

- Completion audit review requires `audited_by`.
- Controlled/test-like source warnings block `goal_complete_candidate`.
- The API never marks the thread goal complete and preserves `does_not_mark_goal_complete=true`.
- Real organization scanner output and real approver identities remain required before final completion.
