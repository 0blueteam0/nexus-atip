# Decision Log

- Decision: 독립 감사 API는 `does_not_mark_goal_complete=true`와 `requires_external_completion_decision=true`를 유지한다.
- Decision: `audited_by`는 필수다.
- Decision: `CASE-V2-*`, fixture, operator-scanner-outputs, prior controlled source warning은 `no_controlled_or_test_source_required` blocker로 처리한다.
