# Decisions

Use the same six default tool IDs as readiness. Keep require_all_named_tools default true. Preserve explicit opt-out for tests or future controlled compatibility only when payload sets require_all_named_tools false.
