# Handoff

Batch safe smoke attestation is implemented.

Read next:

- `attest_safe_smoke_install_version_evidence_candidates` in `runtime/redteam_v2_models.py`.
- `/tool-install-version-evidence/attest-safe-smoke-candidates` in `runtime/redteam_v2_api_router.py`.
- `test_v2_safe_smoke_candidate_batch_attestation_records_multiple_install_evidence` in `tests/test_redteam_v2_api_router.py`.
- `attestRedTeam2SafeSmokeInstallCandidate` in `reports.js`.

Next useful slice: SCA import-only install evidence/workflow or real six-tool collection completion proof.
