# HANDOFF

Next agent should start with `approve_toolchain_collection_evidence` in `runtime/redteam_v2_models.py`. The endpoint approves Evidence candidates from a collection and writes `toolchain-evidence-approvals` artifacts. It does not create Findings or report Claims.
