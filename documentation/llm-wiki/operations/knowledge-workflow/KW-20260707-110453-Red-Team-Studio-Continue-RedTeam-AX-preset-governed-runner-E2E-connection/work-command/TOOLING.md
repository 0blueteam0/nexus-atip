# Tooling

- Frontend: `node --check` for `reports.js`.
- Backend: focused pytest in `tests/test_redteam_v2_api_router.py`.
- Sanity: RedTeam AX frontend runtime and launch readiness contract scripts.
- Git: targeted `git diff --check`.
- Editing: `apply_patch`.
