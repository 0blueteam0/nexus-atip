# Ontology Edges

- RedTeam2 frontend -> preserves -> execution preset `runner_steps`.
- Execution preset API -> provides -> low-risk runner candidates for Trivy and npm audit.
- Governed toolchain runner -> executes -> approved structured steps.
- Collect-results API -> normalizes -> untrusted raw tool output.
- Evidence candidate -> requires -> human approval before Finding or Report claim.
- Full RedTeam AX completion -> still requires -> six-tool live E2E, evidence approval, report export, and completion audit.
