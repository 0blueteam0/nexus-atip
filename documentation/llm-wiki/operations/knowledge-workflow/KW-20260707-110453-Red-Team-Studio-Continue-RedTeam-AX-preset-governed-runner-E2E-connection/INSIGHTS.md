# Insights

- A command-line-only frontend projection loses safety metadata that the backend already computed for each runner step.
- Preserving `runner_steps[]` lets the frontend remain simple for Korean beginner users while still passing structured runner constraints to the backend.
- The change advances the active objective's priority 2 and 3 paths for Trivy/npm audit, but it does not prove full six-tool operational completion.
