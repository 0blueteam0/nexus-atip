# Decision Log

- Default profiler behavior now rejects legacy broad downloads unless explicitly overridden with `--allow-legacy-downloads`.
- Mask/overlay images are not emitted by run_demo; changed_fields remain metadata only.
