# Feedback

User explicitly requested removing mask/block/synthetic-only/submission-invalid visual labels and keeping only anonymized or pseudonymized useful values. The implementation follows that by deleting demo mask/overlay artifacts and keeping changed-fields only in manifest/QC metadata.
