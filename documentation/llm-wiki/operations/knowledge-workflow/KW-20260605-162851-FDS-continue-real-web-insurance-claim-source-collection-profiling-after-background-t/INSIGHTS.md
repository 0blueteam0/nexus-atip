# Insights

Shape-only document profiling is unsafe for broad web image collections because many non-document photos have strong edges/lines. Acceptance must be tied to OCR/vision-passed collection status and negative web context filtering.
