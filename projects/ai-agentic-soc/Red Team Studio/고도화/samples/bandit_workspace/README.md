# Bandit sample workspace

This workspace contains a benign Python helper script for RedTeam AX Bandit
integration smoke testing. It must not contain exploit code, credential
collection logic, destructive file operations, or real secrets.
